import { defineConfig } from 'astro/config';

export default defineConfig({
  // Static output (default). Landing page for willaicite; audit app lives elsewhere.
  site: 'https://willaicite.com',
});
